"""
{{ docstring }}
"""
{% for imp in imports %}
{{ imp }}
{% endfor %}


@dag(
    dag_id="{{ dag_id }}",
    schedule={{ schedule_repr }},
    start_date=datetime(2026, 1, 1),
    catchup=False,
    default_args={"retries": {{ retries }}},
    tags={{ tags_repr }},
)
def {{ dag_id }}():
{{ body }}


{{ dag_id }}()
